Pure Function, no state, receive input -> output
	Always produces the same output for the same input
- AND-gate
	- Y = A & B
- Multiplexer
	- Y = S ? I1 : I0
- Adder
	- Y = A + B
- Arithmetic/Logic Unit ( A complex circuit that peforms various functions (F) on two inputs (A and B), represented as Y = F(A,B) )
	- Y = F(A, B)